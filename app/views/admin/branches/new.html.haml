%h1 New branch

= form_for @branch, url: admin_branch_checkouts_path do |f|
  = render 'common/form_error', record: @branch

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'

= link_to 'Back', repository_path(@orig_branch.repository)
