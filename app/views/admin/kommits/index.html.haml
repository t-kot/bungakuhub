%h1 Listing kommits

%table
  %tr
    %th Message
    %th Revision
    %th
    %th
    %th

  - @kommits.each do |kommit|
    %tr
      %td= kommit.message
      %td= kommit.revision
      %td= link_to 'Show', admin_branch_kommit_path(params[:branch_id], kommit)
      %td= link_to 'Destroy', admin_branch_kommit_path(params[:branch_id], kommit), method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Kommit', new_admin_branch_kommit_path(params[:branch_id])
