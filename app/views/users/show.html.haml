%p
  %b Display name:
  = @user.display_name
%p
  %b First name:
  = @user.first_name
%p
  %b Last name:
  = @user.last_name

= link_to 'Following', user_followings_path(@user)
= link_to 'Follower', user_followers_path(@user)

= render 'common/follow_button', user: @user
